#used when switch-controller disconnection is detected
def reconnect_switch(switch_id):
    """
    Attempts to reconnect a disconnected switch
    """

    print(f"[ACTION] Reconnecting switch {switch_id}")

    # In Mininet, reconnection is often automatic
    # This is a logical placeholder for control-plane action

    print(f"[ACTION] Switch {switch_id} reconnection triggered")
